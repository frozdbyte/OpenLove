# Database path for SQLite
DATABASE_URL="file:./data/openlove.db"

# Web Push VAPID keys (Optional: Auto-generated if not provided)
# PUBLIC_VAPID_KEY=
# PRIVATE_VAPID_KEY=
# VAPID_SUBJECT="mailto:admin@openlove.local"

# Port (default 3000)
PORT=3000
